import '../../data/model/country_model.dart';

class Countries{
  String? name;
  String? capital;
  String? flag;
  Currencies? currency;
  Countries({this.name, this.capital, this.flag,this.currency});
}